package com.byteflipper.random.domain.physics

/** How much work a real-time physical generator may spend on simulation and rendering. */
enum class SimulationQualityTier {
    HIGH,
    BALANCED,
    BATTERY
}
